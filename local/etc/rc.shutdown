#!/bin/oksh

echo "Shutting down all processes.."
killall5 -s KILL

echo "Synching and unmounting filesystems.."
sync
umount -a

case "$1" in
	reboot)
		echo "Rebooting.."
		halt -r
		;;
	
	poweroff)
		echo "Shutting down.."
		halt -p
		;;
esac

wait
